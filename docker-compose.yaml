services:
  tg-btc:
    container_name: tg-btc
    build:
      context: .
    environment:
      BOT_TOKEN: $BOT_TOKEN
      COINMARKET_API_KEY: $COINMARKET_API_KEY
      NAVIDROME_URL: $NAVIDROME_URL
